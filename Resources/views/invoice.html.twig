<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Invoice</title>

    <link href="/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<style>
    #invoice-container {
        padding: 40px;
    }
    .invoice-total {
        font-size: 14px;
        font-weight: bold;
        padding-bottom: 4px;
    }
</style>
<div class="container" id="invoice-container">

    <div class="row">
        <div class="col-md-6" style="padding-left: 25px">
            <h2>Annex Apps Invoice {{ invoiceReference }}</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4" style="padding-left: 25px">
            <strong>Tax date</strong><br>
            {{ invoice.date|date("d M Y") }}
        </div>
        <div class="col-md-4">
            <div class="pull-right">
                <strong>Invoice to:</strong><br>
                {{ tenant.name }}<br>
                {{ tenant.ownerName }}<br>
                {{ tenant.ownerEmail }}
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <table class="table">
                <thead>
                <tr>
                    <th></th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                {% for line in invoice.lines.data %}
                    <tr>
                        <td>
                            {{ app_name }} : {{ line.plan.name }} plan<br>
                            {{ line.period.start|date("d M Y") }} to {{ line.period.end|date("d M Y") }}<br>
                            {{ line.description }}
                        </td>
                        <td style="text-align:right">
                            {{ line.currency|upper }} {{ (line.amount/100)|number_format(2, '.', ',') }}
                        </td>
                    </tr>
                {% endfor %}
                <tr>
                    <td colspan="2">
                        <span class="pull-right invoice-total">
                            Subtotal {{ invoice.currency|upper }} {{ (invoice.subtotal/100)|number_format(2, '.', ',') }}
                        </span>
                        <div style="clear: both"></div>
                        <span class="pull-right invoice-total">
                            Tax {{ invoice.currency|upper }} {{ (invoice.tax/100)|number_format(2, '.', ',') }}
                        </span>
                        <div style="clear: both"></div>
                        <span class="pull-right invoice-total">
                            Total {{ invoice.currency|upper }} {{ (invoice.total/100)|number_format(2, '.', ',') }}
                        </span>
                        <div style="clear: both"></div>
                        <span class="pull-right">
                            {% if invoice.paid %}
                                <span class="label label-success">Paid</span>
                            {% else %}
                                <span class="label label-danger">Not paid</span>
                            {% endif %}
                        </span>

                    </td>
                </tr>
                </tbody>
            </table>

        </div>
    </div>

    <div class="row">
        <div class="col-md-12" style="padding-left: 25px">
            Annex Apps<br>
            12 Dunmore Street<br>
            Bristol<br>
            BS4 2BQ<br>
            United Kingdom
        </div>
    </div>
</div>
</body>
</html>